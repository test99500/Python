import matplotlib.pyplot as plt

fig, ax = plt.subplots()

# plt.subplots() is a function that returns a tuple containing a figure and axes object(s).
# Thus, when using fig, ax = plt.subplots(), you unpack this tuple into the variables fig and ax.[1]

# An Axes object is generated implicitly with the plotted line chart (shown in red). [2]

# References:
# 1. https://stackoverflow.com/a/34162641/14900011
# 2. https://megalodon.jp/2021-0520-0030-00/https://towardsdatascience.com:443/what-are-the-plt-and-ax-in-matplotlib-exactly-d2cf4bf164a9